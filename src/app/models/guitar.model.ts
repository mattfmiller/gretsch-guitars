export class Guitar {
  constructor(public name: string, public imageUrl: string, public category: string, public tailpiece: string, public orientation: string, public series: string, public color: string, public modelNumber: number, public description: string) {}
}
